fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
playwright==1.49.0
patchright==1.49.0
spacy==3.7.6
rapidfuzz==3.10.0
python-dotenv==1.0.1
aiofiles==24.1.0
tldextract==5.1.2
beautifulsoup4==4.12.3
lxml==5.3.0
httpx>=0.27.0
# Local sentiment model (optional — falls back to a lexicon if not installed)
transformers>=4.44.0
torch>=2.2.0
# Postgres driver (psycopg3)
psycopg[binary]>=3.1
# Task queue (optional — falls back to in-process when no broker configured)
celery>=5.4.0
redis>=5.0.0
# Cloudflare R2 / S3-compatible object storage (optional — falls back to local disk)
# S3-compatible object storage (Supabase Storage; optional — falls back to local disk)
boto3>=1.34.0

# Steel.dev managed stealth browser (optional — only needed when STEEL_API_KEY is set)
steel-sdk>=0.18.0
# Supabase JWT validation
python-jose[cryptography]==3.5.0
